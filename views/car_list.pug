extends layout

block content
    h1 Car List

    ul
        for car in car_list
            li
                a(href=`./car/${car._id}`) #{car.name}
                p Manufacturer: #{car.manufacturer.name}
                p Body type: #{car.bodyType.type}
